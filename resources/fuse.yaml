general:
  busy_ports_json_file: "\\resources\\busy_ports.json"
  debug: True


fuse:
  activate_venv: False
  venv_activate_path: "\\orchestra_env\\Scripts\\activate"
  first_port: 5000
  last_port: 6000

services:
  system:
    test_sys_endpoint:
      path: "\\endpoints\\system_endpoints\\py_runnable_endpoint_template.py"
      local: True
      mono: True
  business:
    endpoint_template: "\\endpoints\\py_runnable_endpoint_template.py"
    second_endpoint: "\\endpoints\\py_runnable_endpoint.py"

